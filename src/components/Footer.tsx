import React from 'react'

interface Props {}

export const Footer: React.FC<Props> = () => {
    return (
        <footer className="appFooter">
            · · · − − − · · ·
        </footer>
    );
}
